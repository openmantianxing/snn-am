#include "snn.h"


int main(char* argv){
    // neuron state before nadd instruction
    int seed = atoi(argv);
    // neuron update input voltage
    printf("%d\n", seed);
    printf("%s\n", argv);
    return 0;
}

